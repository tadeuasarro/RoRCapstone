<div class="container-fluid">
  <div class="d-flex justify-content-center">
    <div class="w-50 pt-5">
      <%= form_for(@user, url: users_path, method: 'post') do |f| %>
        <div class="form-group p-3 mb-0">
          <p class="h1 text-dark text-center"><i class="fab fa-twitter"></i></p>
          <p class="h4 text-dark text-center">Sign up</p>
          <div>
            <%= f.label :username, class: 'form-text text-secondary' %>
            <%= f.text_field :username, autofocus: true, class: 'form-control' %>
          </div>
          <div>
            <%= f.label :full_name, class: 'form-text text-secondary' %>
            <%= f.text_field :full_name, class: 'form-control' %>
          </div>
          <div class="mt-3">
            <%= f.submit 'Register', class: 'login-button' %>
          </div>
        </div>
        <div class="d-flex justify-content-end align-items-center">
          <%= link_to 'Back', new_session_path, class: 'no-link text-danger mr-3' %>
        </div>
      <% end %>
    </div>
  </div>
</div>